const express = require('express');
const router = express.Router();

const test_controller = require('../controllers/LikeController');

//router.post("/test", test_controller.test_create);

module.exports = router;